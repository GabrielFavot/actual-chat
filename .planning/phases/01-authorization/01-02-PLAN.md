---
phase: 01-authorization
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/index.ts
  - src/middleware/auth.ts
  - src/types.ts
  - src/utils/logger.ts
autonomous: true

must_haves:
  truths:
    - "Bot starts and listens for messages"
    - "Authorized user ID can trigger bot commands and receive responses"
    - "Unauthorized user ID receives no response (silent ignore)"
    - "Unauthorized attempts are logged for security monitoring"
    - "Bot fails to start if AUTHORIZED_USER_IDS is not set"
    - "/start command shows user's Telegram ID"
  artifacts:
    - path: "src/middleware/auth.ts"
      provides: "Authorization check logic"
      exports: ["authorizedUserIds", "isAuthorized", "authMiddleware"]
    - path: "src/utils/logger.ts"
      provides: "Logging utility for security events"
    - path: "src/index.ts"
      provides: "Bot with authorization middleware and /start command"
  key_links:
    - from: "src/index.ts"
      to: "src/middleware/auth.ts"
      via: "bot.use(authMiddleware)"
      pattern: "bot\\.use\\(authMiddleware\\)"
    - from: "src/middleware/auth.ts"
      to: "process.env.AUTHORIZED_USER_IDS"
      via: "parse and validate env var"
      pattern: "process\\.env\\.AUTHORIZED_USER_IDS"
---

<objective>
Implement authorization middleware that validates Telegram user IDs against an allowlist. Unauthorized users are silently ignored (no response) but attempts are logged for security monitoring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-authorization/01-CONTEXT.md
@.planning/phases/01-authorization/01-01-SUMMARY.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create logging utility</name>
  <files>src/utils/logger.ts</files>
  <action>
Create src/utils/logger.ts with:
- Function logUnauthorizedAttempt(userId: number, username?: string, firstName?: string): void
- Log format: "[UNAUTHORIZED] {timestamp} - User: {userId} | Username: {username || 'N/A'} | Name: {firstName || 'N/A'}"
- Use console.log with ISO timestamp
- Create src/utils directory if needed

Reference: CONTEXT.md specifies logging unauthorized attempts for security monitoring.
  </action>
  <verify>Logger function can be imported and called</verify>
  <done>Logging utility exists and formats output with timestamp</done>
</task>

<task type="auto">
  <name>Create authorization middleware</name>
  <files>src/middleware/auth.ts</files>
  <action>
Create src/middleware/auth.ts with:

1. Parse and validate AUTHORIZED_USER_IDS:
   - Check if process.env.AUTHORIZED_USER_IDS exists
   - If missing, throw error: "AUTHORIZED_USER_IDS environment variable is required"
   - Parse as comma-separated list of numbers
   - If parsing fails, throw descriptive error

2. Export authorizedUserIds: number[] (parsed list)

3. Export isAuthorized(userId: number): boolean (check if in allowlist)

4. Export authMiddleware: MiddlewareFunction for grammY
   - Get user from ctx.from
   - If no user, return (ignore)
   - Check isAuthorized(user.id)
   - If not authorized: call logUnauthorizedAttempt() and return (silent ignore)
   - If authorized: call next() to continue

Create src/middleware directory if needed.
  </action>
  <verify>Auth middleware can be imported and checks user IDs</verify>
  <done>Middleware exists with proper authorization logic and validation</done>
</task>

<task type="auto">
  <name>Integrate middleware and add /start command</name>
  <files>src/index.ts</files>
  <action>
Modify src/index.ts:

1. Import authMiddleware from './middleware/auth'
2. Add middleware: bot.use(authMiddleware)
3. Add /start command:
   - Respond with: "Welcome! Your Telegram ID is: {ctx.from.id}"
   - This helps users find their ID for configuration

4. Remove the echo handler (not needed with auth)

5. Add bot start call at end: bot.start()

The middleware will:
- Silently ignore unauthorized users (no response sent)
- Log unauthorized attempts with user info
- Allow authorized users through to commands
  </action>
  <verify>Bot starts and middleware is applied</verify>
  <done>Bot with working authorization and /start command</done>
</task>

</tasks>

<verification>
- Bot fails to start if AUTHORIZED_USER_IDS not set (check with try/catch)
- Authorized user ID receives response from /start
- Unauthorized user ID receives no response (silent)
- Unauthorized attempts appear in logs
- /start command returns user's Telegram ID
</verification>

<success_criteria>
Bot starts successfully with valid AUTHORIZED_USER_IDS. Unauthorized users are silently ignored with logging. /start command shows user's ID.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authorization/01-02-SUMMARY.md`
</output>
