---
phase: 01-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/index.ts
  - Dockerfile
  - .env.example
autonomous: true
user_setup:
  - service: telegram
    why: "Bot token required to run the bot"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather -> Create new bot -> Copy token"
    dashboard_config:
      - task: "Create bot via @BotFather"
        location: "Telegram app -> Search @BotFather"

must_haves:
  truths:
    - "Bot starts and listens for messages"
    - "Authorized user list is configurable via environment variables"
    - "Bot fails to start if AUTHORIZED_USER_IDS is not set"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies (grammy, dotenv, typescript)"
    - path: "src/index.ts"
      provides: "Bot entry point with grammY initialization"
    - path: "Dockerfile"
      provides: "Container definition for Coolify deployment"
    - path: ".env.example"
      provides: "Environment variable template with required vars"
  key_links:
    - from: "src/index.ts"
      to: "grammy"
      via: "import and initialize Bot"
      pattern: "new Bot.*TELEGRAM_BOT_TOKEN"
    - from: "src/index.ts"
      to: "Dockerfile"
      via: "CMD node dist/index.js"
      pattern: "CMD.*index\\.js"
---

<objective>
Initialize the Node.js project with TypeScript and grammY framework. Create the foundation for the Telegram bot that will run on Coolify.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/phases/01-authorization/01-CONTEXT.md

# Reference for tech stack decisions
</context>

<tasks>

<task type="auto">
  <name>Initialize Node.js project with TypeScript</name>
  <files>package.json, tsconfig.json</files>
  <action>
Create package.json with:
- name: "actual-telegram-notif"
- type: "module" (ESM for grammY)
- dependencies: grammy@^1.x, dotenv@^16.x
- devDependencies: typescript@^5.x, @types/node, ts-node
- scripts: "build" (tsc), "start" (node dist/index.js), "dev" (ts-node src/index.ts)

Create tsconfig.json with:
- target: "ES2022"
- module: "ESNext" 
- moduleResolution: "bundler"
- outDir: "./dist"
- rootDir: "./src"
- esModuleInterop: true
- strict: true
  </action>
  <verify>npm install succeeds without errors</verify>
  <done>package.json and tsconfig.json exist with correct configuration</done>
</task>

<task type="auto">
  <name>Create basic bot entry point</name>
  <files>src/index.ts, .env.example</files>
  <action>
Create src/index.ts with:
- Import Bot from 'grammy'
- Import 'dotenv/config'
- Initialize bot: const bot = new Bot(process.env.TELEGRAM_BOT_TOKEN)
- Add error handling: bot.catch(err => console.error('Bot error:', err))
- Add startup message: console.log('Bot starting...')
- Add basic message handler that echoes: bot.on('message:text', ctx => ctx.reply(ctx.message.text))

Create .env.example with:
- TELEGRAM_BOT_TOKEN=your_bot_token_here
- AUTHORIZED_USER_IDS=comma_separated_user_ids
- ACTUAL_SERVER_URL=http://actual:5006
- ACTUAL_SERVER_PASSWORD=your_password
- BUDGET_ID=your_budget_id
  </action>
  <verify>TypeScript compiles without errors: npx tsc --noEmit</verify>
  <done>Bot can be imported and initialized (will fail at runtime without token)</done>
</task>

<task type="auto">
  <name>Create Dockerfile for Coolify</name>
  <files>Dockerfile</files>
  <action>
Create Dockerfile with:
- FROM node:20-alpine
- WORKDIR /app
- Install build tools: RUN apk add --no-cache python3 make g++
- Copy package*.json
- RUN npm ci --only=production
- Copy source: COPY . .
- Build: RUN npm run build
- USER node
- CMD ["npm", "start"]
  </action>
  <verify>Dockerfile exists and has valid syntax</verify>
  <done>Dockerfile ready for Coolify deployment</done>
</task>

</tasks>

<verification>
- package.json has grammY and dotenv in dependencies
- tsconfig.json configured for ESM TypeScript
- src/index.ts contains basic bot initialization
- Dockerfile exists with node:20-alpine base
</verification>

<success_criteria>
Project compiles without TypeScript errors. Bot can be started with valid TELEGRAM_BOT_TOKEN.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authorization/01-01-SUMMARY.md`
</output>
